<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shf.boot.mapper.MovieSimilarMapper">

    <select id="getSimilar" resultType="com.shf.boot.bean.MovieSimilar">
        select * from moviesimilar where mid = #{id}
    </select>
</mapper>